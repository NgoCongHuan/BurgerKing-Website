@model IEnumerable<BurgerKing.Models.Category>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="content-wrapper">
    <div class="page-header">
        <button id="button-create" type="button" class="btn btn-gradient-primary btn-icon-text">
            <i class="mdi mdi-library-plus btn-icon-prepend"></i>Thêm loại sản phẩm mới
        </button>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="#">Quản lý thông tin</a></li>
                <li class="breadcrumb-item active" aria-current="page">Loại sản phẩm</li>
            </ol>
        </nav>
    </div>
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title" style="font-weight: bold;">Loại sản phẩm</h4>
                    </p>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th style="font-weight: bold;">Mã loại sản phẩm</th>
                                    <th style="font-weight: bold;">Tên loại sản phẩm</th>
                                    <th style="font-weight: bold;">Sửa</th>
                                    <th style="font-weight: bold;">Xóa</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@Html.DisplayFor(modelItem => item.CatId)</td>
                                        <td>@Html.DisplayFor(modelItem => item.CatName)</td>
                                        <td>@Html.ActionLink("Edit", "Edit", new { id = item.CatId })</td>
                                        <td>@Html.ActionLink("Delete", "Delete", new { id = item.CatId })</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
<script>
    $(document).ready(function () {
        $("#button-create").click(function () {
            window.location.href = '@Url.Action("Create", "Categories")';
        });
    });
</script>
